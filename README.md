# Homebrew Distribution Assets

Public, immutable distribution assets for `soundadam/tap`.

Source repositories may be private. Each Release records the originating project,
version, source commit or tag, asset checksum, license boundary, and whether the
artifact is signed or notarized.

This repository contains distribution metadata and Release assets only. It does
not grant official university or vendor status to any package.
